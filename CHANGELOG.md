# 更新记录（方法论与工具链）

内容变更记录在私有内容仓库，本文件只记规则与渲染器的变更。

---

## 2026-09-05　v1.1

### docs：新增 AI 助手入口（AGENTS.md）

- 根目录 `AGENTS.md`：能力自检（实测命令）／档位与产出／规则真源／通用约定／本仓库红线
- `prompt/AGENTS-common.md`：通用工作约定的规范版本，10 条编号固定；各仓库裁剪引用时从「采用第 X、Y 条」即可看出省略了哪几条
- `prompt/AGENT.md` 顶部声明其描述作者本机环境，非通用安装指南；移除私有库章节标题
- 泄漏扫描关键词表统一为 `~/.leakscan-keywords`（软链指向私有库中版本管理的那份）

### style：新增警告行样式；发现并修复 macOS 26 下中文全部丢失

- `build_docx.js` 支持 `<tr class="warn">`：白底 + 左侧红 `C00000` 竖条，
  首列不按短标签列居中。配套约定：`<em>` 只包「警告：」，正文黑字，
  避免整行标红违反第七节的红色总量控制。
- **发现渲染链一直在丢中文**：真实全书转出的 PDF 里中文一个字都没有，
  只剩 ASCII，而表格、底纹、红色、竖条全部正常——目视极易漏掉。
  根因是 macOS 26 把 PingFang 等移入 `/System/Library/AssetsV2/`，
  LibreOffice 只扫经典字体路径时解析不到任何中文字体。
  影响面：`sync.sh` 的排版校验与所谓「渲染目检」一直跑在看不见中文的 PDF 上。
- `SKILL.md` 第六节补 `warn` 标记、第七节补警告行规格与 macOS 26 字体陷阱。

### rule：合并 Codex 侧并行分支的独有规则

`~/.codex/skills/flight-theory-notes`（无版本管理，v1.8，最后更新 09-05 01:27）
与本仓库是同一晚并行演进的两条线，从未互相同步。经逐项比对与实证核实后，
把其中本仓库缺失且仍有效的规则并入：

- 新增 `prompt/standing-decisions.md`：SD-1…SD-12 长期决策 + 待确认参数 + 历史决策
- `SKILL.md` 新增「一之二 长期决策」入口段
- `SKILL.md` 新增「三之二 图片型原始材料」：图一律转成文字、5 类原图映射表、
  `［原图：… — 建议手动插入］` 占位约定
- `SKILL.md` 第四节补 8/9/10 三小节：父条件下多个子条件、章节内知识体系顺序、
  大节内部的块编号
- `SKILL.md` 第七节补：表格尺寸判据（SD-4/SD-5 的执行细则）、排版标杆的使用（SD-10）
- `SKILL.md` 新增「七之二 笔记库治理」：最高边界表、第 0 章同步义务、
  先盘点后产出的两阶段流程、inbox 增量维护

实证否定并记入「历史决策」的四项：5 章重构方案（用户确认放弃）；
条目级 `[NG]`/`[738]` 标记（全库 0 处，实际用全角【】126 处）；
块编号并入库后作废（与实际相反，3.6／3.8 两节仍在用）；
结尾语、`EAEFF3` 共享项色、模式 A 块标题条（全库 0 处且渲染器未实现）。

### style：check_src.py 新增表格列数一致性检查

- `build_docx.js` 的 `nCols` 只按**首行** `colspan` 合计推算。按本项目格式约定，
  首行常是 `class="premise"` 通栏行——它的 colspan 若与表格真实列数不符，
  整张表的列宽会被静默算错，且 `check_src.py`（原本零 colspan 校验）与
  `verify.py`（只查空白页／标签泄漏／项目符号／front matter）都不会发现。
- 新增检查：首行 colspan 合计 ≠ 表内最大列数即报错并给出行号。
- 实测：现有 81 节 785 个表块**零误报**；注入首行 colspan 错误能精确定位；
  修正后放行。

### style：check_src.py 列数检查改按 `<table>` 边界切分

- 原实现按空行切分源文本。实测现有 785 个表均无内部空行，故无误报；
  但表内一旦出现空行就会被拆成两个假表并误报。改为按 `<table>…</table>`
  切分，与 `build_docx.js` 界定表格的方式一致。
- 四条路径实测：81 节全过／首行 colspan 错误报错并定位／表内含空行且
  colspan 正确时放行（旧实现会误报）／表内含空行且 colspan 错误仍报错。

### docs：第七节补记两个未记录的渲染行为

- 模块速查导航表：`%%NAV%%`…`%%ENDNAV%%`，页码走 `PAGEREF` 域，
  书签 `SEC_x_y` 只对一、二级标题生成
- 独立成行的「详见 …」渲染为 8pt 灰字

### fix：第九节 commit 前缀规则与实际不符且指向泄漏

- 原「前三个推主库，`style:`/`rule:` 推工具链」与 `AGENT.md`「前四个推私有库」
  互相矛盾。实测两库各用过 4 次 `docs:`，`fix:`/`feat:` 两边都出现，`chore:`
  在主库用过 4 次而两份文件都未规定其归属——两条规则都与实际不符。
  按 SKILL.md 原文推断，在私有库写文档时 `docs:` 不属「前三个」，会被推向公开库。
- 改为：前缀只表示改动性质，**推哪个仓库由改动文件所在仓库决定**；
  一次提交只动一个仓库的文件。`AGENT.md` 改为引用本节，不再各自表述。
- 第九节「工具链放 SKILL.md 副本」改为「SKILL.md（规则真源，不是副本）」——
  主库根本没有 SKILL.md，工具链那份是原件，与 `AGENT.md` 的「规则真源」一致。
- 泄漏扫描改为直接引用统一关键词表 `~/.leakscan-keywords`，并写明表不存在时
  停下报告、不得当作通过。

### fix：文档与实现不符

- `README.md` 工具清单与用法块补上 `check_src.py`，并说明两套校验都要跑——原用法只跑 `verify.py`，与 SKILL.md「不可互相替代」冲突
- `README.md` 的 `CH` 字典改为 `MOD`（实际变量名，`CH` 不存在）
- `tools/sync.sh` 帮助文本去掉硬编码的「534 页」——那是字体缺失环境下的数字，与「报页数必须带字体条件」矛盾
- `prompt/SKILL.md` 第一节：0.x 与详章由「两边互指」改为「详章单向指向 0.x」，与第六节「0.x 不放详见」及全部 81 节实际做法一致
- `examples/sample_section.md` 的示例数值占位化（标签已占位、数字留真属不一致）

---

## 2026-09-04　v1.0

### rule：整理规则成型（prompt/SKILL.md）

- 确立两段式工作流：整理成标准节 → 判定归属并入主库对应章节
- 归属判定三判据：来源性质 / 有无机理 / 拿不准交使用者裁定
- 内容忠实三底线：不丢信息、不改含义、不臆测添加；数值逐字保留；安全关键数据只标不改
- 结构方法：先找主判据、三层信息下沉、五段骨架、抽象分类必配示例列、四类表格形态、列内语义一致性
- 重复处理：章内重复整合留指针，跨章重复分工保留双向「详见」，合并取并集
- study 文档 vs 执行速查表的繁简分野
- 源格式约定：markdown + 内嵌 HTML 表格，六个标记；行首禁用项目符号；同名标签不可自嵌

### style：渲染器（tools/build_docx.js）

- 递归解析嵌套标记，修复 `<strong>…<em>X</em></strong>` 内层标签被当字面文本打印
- rowspan 占位网格：列宽计算与单元格渲染共用同一套真实起始列推算
- 列宽策略：短列按内容定宽，余量按比例给说明类长列
- 对齐策略：按列判定，短列与数值列整列居中，长列左对齐
- 表格不跨页：能放下的整表保持；超页逐级缩至 8.5/8/7.5/7pt，仅在真能塞下时才缩
- 分页改用 `pageBreakBefore`，消除空段落导致的空白页；表格后接标题时不补间隔段落
- 标题注册为标题 1–4 样式，统一纯黑加粗
- 封面：粗黑线 + 小初号主标题 + 副标题 + 细线 + 作者行（`DOC_AUTHOR` / `DOC_SUBTITLE` 环境变量），封面不显示页码
- 页码右下角小五号，只显示「第 X 页」
- 目录用 `TOC \h \o "1-2"` 域，超链接，章 + 全部小节

### add：工具

- `assemble.py` 源文件 → 章节 md / 全书 md
- `docx2md.py` 既有 docx → 源格式，无损保留加粗、红字（C00000）、合并单元格
- `verify.py` PDF 校验：空白页 / 标签泄漏 / 项目符号残留 / front matter 泄漏
