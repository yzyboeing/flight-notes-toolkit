# 跨 AI 验证记录

此文件区分包结构、环境探测、脚本执行、模型行为与成品验收。不能用某一层通过推导其他层通过。

## 2026-09-05：基础验证

- 六个源码包的标准 Skill 结构检查通过。
- 六个仓库合计 30 个单元测试通过，其中分发工具 6 个测试覆盖确定性、允许列表、提交隔离、安装、重复安装拒绝、哈希破坏、危险 ZIP 和阶段隔离。
- 翻译包新增 2 个测试，区分远程 Markdown 链接与本地引用、验证 Skill 分发来源哈希的有限例外；原文档哈希禁入规则保留。
- 工具库和翻译包的仓库外私有关键词检查通过。该结果不是所有隐私风险排除证明。
- Claude 桌面版被发现且已登录，但在独立测试消息确认提交前，模型行为测试仍记为未完成。网页版需要重新登录。

以上为当前执行环境中的代码和包结构测试，不是六个 Skill 的端到端任务验收，也不是跨模型准确率。私有公司规则不用于第三方 AI 测试。

## 本机安装包实测

本次六个候选包均经过构建、ZIP 校验、安全解包安装、标准 Skill 验证和 rules 阶段探测。使用 Python 3.12 环境，安装目标为隔离目录，不覆盖用户原有安装。两份文档包在安装后重新通过各自完整包检查，Node 依赖按 lockfile 安装。

| 范围 | 已执行 | 未覆盖 |
|---|---|---|
| flight-theory-notes | 公开虚构样例源检查、组装、DOCX 制作、PDF 转换；3 页渲染均已目视检查，中文和表格正常 | Word 目录域未刷新，目录页只有标题；没有私有笔记全书验收 |
| flight-doc-translate | 936 条术语 / 26 个行为用例 / 20 个触发用例的结构校验；文档工具发现；字体辅助脚本实际用于上述渲染 | 没有真实航空原文翻译或模型准确率测量 |
| feixun-ppt | 本地资源配置与 PPTX 结构检查通过 | 未制作课件，未向 Claude 发送私有规则或母版 |
| jimmyloon / jimmysurge | 安装包内检查器对各自当前脱敏 V1.0 输入运行通过，输入未修改 | 未导入客户端，未做设备网络测试 |
| vbs | 保留同期远端共享运行环境更新；阶段发现通过；共享模型文件深度校验通过 | 未运行真实语音转写或烧录视频；原有审核关口未跳过 |

默认系统 Python 版本不一定满足要求；使用真正执行任务的解释器跑 doctor。模型已经配置与模型准确转写是两件不同的事。

## AI 行为兼容性状态

| 平台 | 本次状态 | 证据边界 |
|---|---|---|
| Codex 当前本地会话 | 上述脚本、安装与渲染实测通过 | 不是独立盲测，不据此判定所有规则行为通过 |
| Claude 桌面版 | 已发现登录会话，独立测试提交未确认；等待空白新会话 | 旧聊天不计入本次证据，不能称为兼容性通过 |
| Claude 网页版 | 测试入口需要重新登录 | 未运行 |
| 其他 AI | 未测试 | 不声明兼容 |

可重复测试题与记录格式见 [tests/cross-ai-cases.md](tests/cross-ai-cases.md)。
