# 长期决策与待确认参数

本文件每次必读，只保存跨文档长期有效的决策，不保存某一份材料的技术事实、数值或程序。优先级为：`fidelity.md` 硬约束 > 本文件 > 其他参考文件。

已确认的问题不得重复询问。决策变化时更新原条目和日期，被推翻的条目移入“历史决策”。

## 已确认决策

| 编号 | 决策 | 确认日期 |
|---|---|---|
| SD-1 | 成品为带自动目录的 `.docx`，A4 横版 | 2026-09-04 |
| SD-2 | 成品不插图、不绘图；重要图只留手动插入占位 | 2026-09-04 |
| SD-3 | 笔记内容不上公开仓库；通用规则和脚本可以公开 | 2026-09-04 |
| SD-4 | 表格尺寸和列宽按内容职责及最长有意义信息单元自适应，不按累计字数分配：序号/短标签列压窄；首列完整项目名尽量保持一行；多条并列句按最长并列项估宽并可适当收窄；整表内容少时按实际所需宽度收缩 | 2026-09-04 |
| SD-5 | 对齐以简洁美观和扫读自然为准：表头、序号、数值、短标签可居中，说明性长文和条件叙述左对齐；同列保持一致；并列子条件按原笔记顺序用 ①②③ | 2026-09-04 |
| SD-6 | 章节名称与条目标题保留原稿命名，不用新的归纳名、块名或编号替换 | 2026-09-04 |
| SD-7 | 表格逐格校验：原格有内容则成品对应格不得为空；独立的短横线占位符也必须保留；PDF 边线误识别出的整列无源文字尾列应裁除，不得在成品中留下细长假空白列 | 2026-09-04 |
| SD-8 | 默认保持原顺序；用户要求重新梳理逻辑时，允许同一一级章节内按知识体系重排既有板块，无需另问章内移动权限；标题原名不改，跨一级章节不移动 | 2026-09-04 |
| SD-9 | 源页标题转成 Word 标题后只保留一次，不在其上下重复输出完全同名的标题行；只按完全一致去重，不得把“自动飞行操作”这类含有标题词但技术含义更具体的行当成重复标题删除 | 2026-09-04 |
| SD-10 | 成品按出版物标准统一字体、层级、间距、表格、分页和留白，不为美化新增内容；若用户提供排版标杆，只迁移可验证的视觉规范，既定内容与自适应表格规则优先 | 2026-09-04 |

## 待确认参数及默认值

- **Markdown 表格写法**：默认使用内嵌 HTML `<table>` 加 class。若用户改选 YAML 描述，再调整构建脚本。
- **跨章交叉引用**：默认只使用 front matter 的 `related` 字段，正文不加容易因重编号失效的“相关内容见 X.X”。
- **机型区分迁移**：默认保留原稿已有机型措辞，只在条目级增加 `[NG]`、`[738]`、`[NG+738]` 或 `[待确认]`。
- **第 4 章技术提示**：默认保留为独立沉淀区，不按主题打散。

采用默认值时不必中断工作，但必须在交付说明中提醒用户。

## 历史决策

暂无。
