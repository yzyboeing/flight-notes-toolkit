# AGENTS.md — flight-notes-toolkit

任何 AI 助手接手本仓库时**首先读本文件**。它只讲「你是谁、能做到哪一步、规则在哪、不能做什么」，不重复整理规则本身。

## 一、能力与验证

按任务检查文件、脚本或文档生成能力；仅维护规则或工具时无需启动完整文档制作环境。缺失能力只限制相关步骤，不把文本草稿称为已验证 DOCX。

- `docx` 依赖通常位于笔记项目或共享运行时，按实际路径加载；不假定本仓库自带依赖。
- `check_src.py` 没有 `--help`，且默认查当前目录的 `notes_src`。使用 `python3 tools/check_src.py --src <笔记仓库>/notes_src [--quiet]`，不在工具仓库空跑。
- 交付 DOCX 仍须源检查、结果检查和实际渲染目检。未执行的检查如实说明。

## 三、规则真源

| 文件 | 内容 | 注意 |
|---|---|---|
| `prompt/rules.md` | 整理规则真源——归属判定、结构方法、标记约定、排版规则 | 整理笔记内容时读取；工具维护只读相关章节 |
| `prompt/standing-decisions.md` | **长期决策记录（SD-1…）** | 整理笔记时读取；已有结论不得重复提问；「历史决策」中已放弃的方案不得重新提出 |
| `prompt/AGENT.md` | 简短任务入口 | 引导读取规则并实测能力 |
| `README.md` | 对外说明 | |

兼容 raw 入口仍为 `https://raw.githubusercontent.com/yzyboeing/flight-notes-toolkit/main/prompt/SKILL.md`，由 `tools/export_prompt.py` 自动生成。

三者冲突时以 `prompt/rules.md` 为准。

## 四、执行约定

遵守用户停止点和本 Skill 的版本审核关口；在已授权范围内完成任务，不重复询问。仅用户明确要求时委派子代理。凭据由用户本地处理；如实报告实际验证和未覆盖项。风险在首次相关操作前说明，条件未变时不重复。

## 五、本仓库红线

1. **本仓库是公开的。推送前必须跑泄漏扫描。**
   ```bash
   python3 tools/check_package.py --private-keywords ~/.leakscan-keywords
   ```
   关键词表刻意放在仓库外——把要防的词列进公开仓库等于公布它防的是谁。**关键词表不存在时停下报告，不得当作通过。**
2. **笔记正文、私有库的章节标题、具体运行限制值不进本仓库。** 本仓库只放方法论与工具。
3. **数值逐字保留**：不换算、不四舍五入、不「修正」看起来异常的值。
4. **交付笔记 DOCX 时两套校验都要跑**：`check_src.py` 查源头，`verify.py` 查排版结果。只跑一套会漏——源码干净不代表排版没问题，反过来也一样。
5. **推送前先运行包检查、自动导出一致性检查，并 `git fetch` 比对远端。** 远端可能有本地没有的提交，不要用本地旧文件覆盖远端新内容。
6. **删文件前先问用户**，或改名备份代替删除。
