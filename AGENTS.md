# AGENTS.md — flight-notes-toolkit

任何 AI 助手接手本仓库时**首先读本文件**。它只讲「你是谁、能做到哪一步、规则在哪、不能做什么」，不重复整理规则本身。

## 一、先实测能力，不要假设

依次执行，**不要凭文档描述判断**：

```bash
ls tools/            # 能读文件？
python3 --version    # 有 Python？
node --version       # 有 Node？
```

三条全过 → **完全模式**。任一条失败 → **草稿模式**。

两点须知，否则会误判：

- **本仓库不含 `node_modules`。** `docx` 渲染依赖装在笔记仓库里，渲染能力要到那边自检，不要在本仓库 `require('docx')`。
- **`check_src.py` 没有 `--help`**，且默认在当前目录找 `notes_src`。本仓库没有笔记正文，直接跑必然报错。正确用法是 `python3 tools/check_src.py --src <笔记仓库>/notes_src [--quiet]`。

**永远不要编造执行结果。没跑就说没跑。** `prompt/AGENT.md` 里「依赖已装好」「gh CLI 已登录」只在作者本机为真，不是通用事实。

## 二、两个档位各能做到哪一步

| 能力 | 完全模式 | 草稿模式 |
|---|---|---|
| 读规则、解释规则 | 是 | 是 |
| 按格式产出一节笔记源文件文本 | 是 | 是 |
| 改工具、改规则 | 是 | 只能给改后全文，由用户替换 |
| 跑 check_src.py / verify.py | 是 | 否，须声明「未校验」 |
| 生成 docx | 是 | 否，不得用文本冒充成品 |
| commit / push | 是，见第五节前置条件 | 否 |

草稿模式**开场第一句**就告诉用户：「我没有本机权限，只能给源文件文本，未经任何校验。」

## 三、规则真源

| 文件 | 内容 | 注意 |
|---|---|---|
| `prompt/SKILL.md` | 整理规则真源——归属判定、结构方法、标记约定、排版规则 | 动手前完整读一遍 |
| `prompt/AGENT.md` | 作者本机的操作流程 | 描述的是作者那台 Mac，其中环境事实不通用 |
| `README.md` | 对外说明 | |

公开 raw 入口：`https://raw.githubusercontent.com/yzyboeing/flight-notes-toolkit/main/prompt/SKILL.md`

三者冲突时以 `prompt/SKILL.md` 为准。

## 四、通用工作约定（本仓库适用部分）

- 回答前先检查前提：未经证实的前提、逻辑跳跃、关键信息缺失、概念混淆、把推测当事实。有问题先指出，不沿错误前提往下推。
- 明确区分：已确认事实／合理推测／建议／无法核实。无法核实就说无法核实，不猜、不编。
- 尊重用户明确设定的停止点。未指定停止点时正常做完，不自己发明审批关口。
- 本次任务已确认的范围与选择，在输入未变化时持续有效，不重复询问。
- 不处理密码、token 或登录流程。git 认证靠用户已配置的凭据。
- 不使用子代理、委派或并行代理，除非用户明确要求。

> 以上采用 `AGENTS-common.md` 的第 1、2、4、5、7、8、9 条。规范版本见公开仓库 `flight-notes-toolkit/prompt/AGENTS-common.md`（raw：`https://raw.githubusercontent.com/yzyboeing/flight-notes-toolkit/main/prompt/AGENTS-common.md`）。本节是按本仓库裁剪的副本——**条目编号固定**，从上面这行就能看出哪几条被省略了。修改总约定后需回头核对五份。

## 五、本仓库红线

1. **本仓库是公开的。推送前必须跑泄漏扫描。**
   ```bash
   grep -rnf ~/.leakscan-keywords --exclude-dir=.git .
   ```
   关键词表刻意放在仓库外——把要防的词列进公开仓库等于公布它防的是谁。**关键词表不存在时停下报告，不得当作通过。**
2. **笔记正文、私有库的章节标题、具体运行限制值不进本仓库。** 本仓库只放方法论与工具。
3. **数值逐字保留**：不换算、不四舍五入、不「修正」看起来异常的值。
4. **两套校验都要跑**：`check_src.py` 查源头，`verify.py` 查排版结果。只跑一套会漏——源码干净不代表排版没问题，反过来也一样。
5. **推送前先 `git fetch` 比对远端。** 远端可能有本地没有的提交，不要用本地旧文件覆盖远端新内容。
6. **删文件前先问用户**，或改名备份代替删除。
